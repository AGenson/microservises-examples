constants {
	State_Closed = "Closed",
	State_HalfOpen = "Half Open",
	State_Open = "Open"
}

constants {
	CallTimeout = 20,
	ResetTimeout = 30,
	TripThreshold = 5,
	RollingWindow = 60 // Need to implement it in some way, it's not used
}
